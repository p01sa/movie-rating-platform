# movie-rating-platform
Movie Rating Platform is a Node.js and Express.js application with JWT authentication and role-based access. Admins can add, edit, and delete movies, while users can register, log in, and browse the movie list. The system uses SQLite for data storage and includes secure token-based login. Admin credentials: admin@admin.com / j12nd0.
